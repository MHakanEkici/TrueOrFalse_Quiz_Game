import 'package:flutter/material.dart';

const Icon trueIcon = Icon(Icons.check_circle_outline, color: Colors.green, size: 30,);
const Icon falseIcon = Icon(Icons.cancel_outlined, color: Colors.red, size: 30,);